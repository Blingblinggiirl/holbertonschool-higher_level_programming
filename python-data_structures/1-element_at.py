#!/usr/bin/python3
def element_at(my_list, idx):if idx < 0:
        if idx < 0:
            return my_list
        for i in range(0, len(my_list)):
            if i == idx:
                my_list[i] = element
        return element
